{% extends 'base.html.twig' %}

{% block title %}Fuseauhoraire index{% endblock %}

{% block body %}
    <h1>Fuseauhoraire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fuseau</th>
                <th>Utc</th>
                <th>Ville_en</th>
                <th>Ville_fr</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fuseauhoraire in fuseauhoraires %}
            <tr>
                <td>{{ fuseauhoraire.id }}</td>
                <td>{{ fuseauhoraire.fuseau }}</td>
                <td>{{ fuseauhoraire.utc }}</td>
                <td>{{ fuseauhoraire.villeEn }}</td>
                <td>{{ fuseauhoraire.villeFr }}</td>
                <td>
                    <a href="{{ path('fuseauhoraire_show', {'id': fuseauhoraire.id}) }}">show</a>
                    <a href="{{ path('fuseauhoraire_edit', {'id': fuseauhoraire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fuseauhoraire_new') }}">Create new</a>
{% endblock %}
